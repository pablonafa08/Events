import { FiltereventPipe } from './filterevent.pipe';

describe('FiltereventPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltereventPipe();
    expect(pipe).toBeTruthy();
  });
});
